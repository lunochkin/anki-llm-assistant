$schema: "http://json-schema.org/draft-07/schema#"
type: object
properties:
  kind:
    type: string
    enum: ["deck_list"]
    description: "Message type identifier"
  decks:
    type: array
    items:
      $ref: "deck.schema.yaml"
    description: "List of decks"
required: ["kind", "decks"]
