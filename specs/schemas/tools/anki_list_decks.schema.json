{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "anki_list_decks Input Schema",
  "description": "Input parameters for listing Anki decks",
  "type": "object",
  "properties": {
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10,
      "description": "Maximum number of decks to return"
    }
  },
  "required": ["limit"],
  "additionalProperties": false
}
