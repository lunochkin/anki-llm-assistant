runtime:
  framework: langchain
  agent_style: react
  model_env: OPENAI_MODEL            # e.g. gpt-4o, gpt-4o-mini, gpt-5, etc.
  temperature: 0                     # deterministic responses
adapters:
  anki_url: http://127.0.0.1:8765  # AnkiConnect API endpoint
